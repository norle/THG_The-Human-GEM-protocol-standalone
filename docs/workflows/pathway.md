# Pathway workflow

`pathway` applies an explicit pathway definition and identifier database to a
copied source model. The pathway plan, application report, validation report,
provenance, and JSON/XML exports are retained in the resumable run.

Run it with `thg-run pathway CONFIG`. The source model is an explicit input and
is never described as a β1, β2, or Final-THG artifact.
